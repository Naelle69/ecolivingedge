{% extends 'base.html.twig' %}

{% block title %}EcoLiving Edge{% endblock %}

{% block body %}

 
        <div class="row">
            <div class="col-sm-3">
                {% if app.user %}
                    <div class="mb-3">
                        Vous êtes connecté en tant que {{ app.user.name }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
                        {# <div class="col-sm-2"><img src="{{ asset('images/avatar.jpg') }}" width="100" /></div> #}
                    </div>
                    
                {% else %}
                    <div class="mb-3">
                        Vous n'êtes pas connecté. <a href="{{ path('app_login') }}">Connexion</a>
                    </div>
                {% endif %}
            </div>
        </div>



        {% include "Include_components/Nos_avantages.html.twig" %} 
        {% include "Include_components/Favorite_product.html.twig" %} 



{% endblock %}
