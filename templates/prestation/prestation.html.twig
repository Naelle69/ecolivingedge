

{% extends 'base.html.twig' %}

{% block title %}Hello PrestationController!{% endblock %}

{% block body %}
    <div class="grid">

    <div class="container">
        <h1>Nos services</h1>
        <div class="row">
            {% for prestation in prestation %}
                <div class="col-12 col-md-6 col-lg-4 mb-4">
                    <div class="card text-center">
                        <div class="card-header">{{ prestation.name }} Image</div>
                        <div class="card-body">
                            <h5 class="card-title">{{ prestation.name }}</h5>
                            <p class="card-text">{{ prestation.description }}</p>
                            <p class="card-text">{{ prestation.price }}€</p>  
                        </div>
                        <div class="card-footer text-muted">
                            <a href="#" class="btn btn-primary">Nous contacter</a>
                            <a href="#" class="btn btn-primary">Commenter</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>


    </div>    
{% endblock %}
