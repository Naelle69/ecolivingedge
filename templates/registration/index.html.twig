{% extends 'base.html.twig' %}

{% block title %}Incription{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
    <div class="col-sm-4">
    </div>
        <div class="col-sm-4">
         <h1>Inscription</h1>

        {{ form_start(registration) }}
            {{ form_row(registration.email, {
                label: 'Adresse email'
            }) }}
            {{ form_row(registration.name, {
                label: 'Nom'
            }) }}
            {{ form_row(registration.plainPassword, {
                label: 'Mot de passe'
            }) }}
            {{ form_row(registration.agreeTerms) }}
            <button href="{{ path('user_profil', {'id': 1}) }}" class="btn btn-primary">Inscription</button>
            <button href="{# {{ path('user_profil', {'id': 1}) }} #}" class="btn btn-primary">Mot de passe oublié</button>
        {{ form_end(registration) }}
        
        </div>
         <div class="col-sm-4">
    </div>
    </div>
</div>
   
{% endblock %}
