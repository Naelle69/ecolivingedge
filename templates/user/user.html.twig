{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <div class="container">
{#         <div class="row">
            <div class="col-sm-3">
                <a>Mes infos</a>
                {% if app.user %}
                    <div class="mb-3">
                        Vous êtes connecté en tant que {{ app.user.name }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
                        
                    </div>
                    
                {% else %}
                    <div class="mb-3">
                        Vous n'êtes pas connecté. <a href="{{ path('app_login') }}">Connexion</a>
                    </div>
                {% endif %}
            </div>
        </div> #}
{# <div class="col-sm-2"><img src="{{ asset('images/avatar.jpg') }}" width="100" /></div> #}
        <!-- Tabs navs -->
        <ul class="nav nav-tabs nav-fill mb-3" id="ex1" role="tablist">
            <li class="nav-item" role="presentation">
                <a
                    class="nav-link active"
                    id="ex2-tab-1"
                    data-bs-toggle="tab"
                    href="#ex2-tabs-1"
                    role="tab"
                    aria-controls="ex2-tabs-1"
                    aria-selected="true"
                >Mes informations personnelles</a>
            </li>
            <li class="nav-item" role="presentation">
                <a
                    class="nav-link"
                    id="ex2-tab-2"
                    data-bs-toggle="tab"
                    href="#ex2-tabs-2"
                    role="tab"
                    aria-controls="ex2-tabs-2"
                    aria-selected="false"
                >Mon historique d'achats</a>
            </li>
            <li class="nav-item" role="presentation">
                <a
                    class="nav-link"
                    id="ex2-tab-3"
                    data-bs-toggle="tab"
                    href="#ex2-tabs-3"
                    role="tab"
                    aria-controls="ex2-tabs-3"
                    aria-selected="false"
                >Mes messages et commentaires</a>
            </li>
        </ul>
        <!-- Tabs navs -->

        <!-- Tabs content -->
        <div class="tab-content" id="ex2-content">
            <div
                class="tab-pane fade show active"
                id="ex2-tabs-1"
                role="tabpanel"
                aria-labelledby="ex2-tab-1"
            >
            {{ form_start(form) }}
                {{ form_row(form.name, {
                    label: 'Nom'
                }) }}            
                 {# {{ form_row(form.email, {
                    label: 'Adresse email'
                }) }} #}

                {#{{ form_row(form.password, {
                    label: 'Mot de passe'
                }) }} #}
            {{ form_end(form) }}
            </div>

            <div
                class="tab-pane fade"
                id="ex2-tabs-2"
                role="tabpanel"
                aria-labelledby="ex2-tab-2"
            >
                Tab 2 content
            </div>
            
            <div
                class="tab-pane fade"
                id="ex2-tabs-3"
                role="tabpanel"
                aria-labelledby="ex2-tab-3"
            >
                Tab 3 content
            </div>
        </div>
        <!-- Tabs content -->
    </div>

    <!-- Inclusion des scripts nécessaires -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>

{% endblock %}



